<?xml version="1.0" encoding="utf-8"?>
<vn.vhn.vhscode.ui.AnimatedSessionListViewItemLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/session_item_background"
    android:divider="@android:color/transparent"
    android:dividerHeight="0dp"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5sp"
        android:gravity="start|center_vertical"
        android:divider="@android:color/holo_red_dark"
        android:dividerHeight="2dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="@dimen/session_item_icon_size"
            android:layout_height="@dimen/session_item_icon_size" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/session_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="marquee"
            android:gravity="start|center_vertical"
            android:padding="6dip"
            android:textColor="@color/white"
            android:textSize="@dimen/session_item_text_size" />
    </LinearLayout>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/session_meta"
        android:visibility="gone"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="marquee"
        android:gravity="start|center_vertical"
        android:padding="6dip"
        android:textColor="@color/white"
        android:textSize="@dimen/session_item_text_size" />

</vn.vhn.vhscode.ui.AnimatedSessionListViewItemLayout>
